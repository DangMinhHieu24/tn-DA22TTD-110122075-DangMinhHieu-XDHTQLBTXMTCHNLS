name: admin
description: Admin dashboard feature module
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../core
  design_system:
    path: ../../design_system
  auth:
    path: ../auth
  
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  fl_chart: ^0.68.0
  intl: ^0.20.2
  dio: ^5.4.0
  dartz: ^0.10.1
  get_it: ^7.6.4
  share_plus: ^10.0.0
  image_picker: ^1.1.2
  supabase_flutter: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
